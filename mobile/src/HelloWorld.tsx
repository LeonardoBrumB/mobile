import React from 'react';
import{Text, View} from 'react-native';

const HelloWorld = () => {
    return <Text style = {{color: 'black', fontSize: 25}}>Hello World</Text>;
};

// (parametro: number) => {console.log('opa bão?!')};

export default HelloWorld;