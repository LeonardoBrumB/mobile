import { FirebaseFirestoreTypes } from "@react-native-firebase/firestore";

type INotas = {
    id: string,
    descricao: string,
    created_at: FirestoreError.fieldValue.serverTime
}

export type INotas;